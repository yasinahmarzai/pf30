#include <iostream>
#include <cmath>
using namespace std;

void q24() {
    float a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;
    float total = a + b + c + d + e;
    cout << total << endl << total / 5 << endl;
}


int main(){
    q24();
    return 0;
}
